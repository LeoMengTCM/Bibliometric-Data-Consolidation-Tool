# 快速使用指南

## 一分钟上手

### 推荐第一次运行

仓库自带可复现实例：

```text
Example/
├── wos.txt
└── scopus.csv
```

先跑一遍本地 WOS 对齐逻辑：

```bash
python3 run_ai_workflow.py --data-dir Example --no-ai
```

如果你要直接跑默认完整流程：

```bash
python3 run_ai_workflow.py --data-dir Example
```

## 你会得到什么

默认工作流会依次完成：

1. Scopus 转 WOS 风格格式
2. 用当前 WOS 语料校准转换和 `C3` 对齐
3. WOS + Scopus 合并去重
4. 语言筛选
5. 机构清洗
6. 统计分析与图表生成

## 数据目录要求

你的数据目录需要包含：

```text
/你的数据目录/
├── wos.txt
└── scopus.csv
```

## 常用命令

基础运行：

```bash
python3 run_ai_workflow.py --data-dir "/path/to/data"
```

关闭 AI 分支：

```bash
python3 run_ai_workflow.py --data-dir "/path/to/data" --no-ai
```

关闭机构清洗：

```bash
python3 run_ai_workflow.py --data-dir "/path/to/data" --no-cleaning
```

指定年份范围：

```bash
python3 run_ai_workflow.py --data-dir "/path/to/data" --year-range 2015-2024
```

筛选中文文献：

```bash
python3 run_ai_workflow.py --data-dir "/path/to/data" --language Chinese
```

查看帮助：

```bash
python3 run_ai_workflow.py --help
```

## 关于 `--no-ai`

`--no-ai` 只关闭 AI 分支，不关闭当前最关键的本地逻辑。

它仍然保留：

- 本地 Scopus → WOS 风格转换
- 当前 WOS 语料校准
- 本地基于重复对的 `C3` 恢复
- 合并 / 去重 / 筛选 / 清洗 / 分析

## 输出文件

运行完成后，数据目录下通常会出现：

- `scopus_converted_to_wos.txt`
- `scopus_enriched.txt`（启用 AI 时）
- `merged_deduplicated.txt`
- `english_only.txt` 或其他语言筛选结果
- `Final_Version.txt`（启用机构清洗时）
- `*_analysis_report.txt`
- `ai_workflow_report.txt`
- `Figures and Tables/`

## 当前验证快照

基于仓库自带示例，当前本地 round11 已把 100 组重复文献中的 `C3` 差异从 `24` 降到 `12`。

## GUI 使用

```bash
python3 gui_app.py
```

也可以双击项目根目录下的 `启动GUI.command`。
