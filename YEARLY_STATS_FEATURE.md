# 年度统计功能说明

## 功能概述

在合并去重报告中新增了**年度发文量及引文量统计表格**，帮助研究者快速了解文献的时间分布趋势和引用情况。

---

## 功能特性

### 统计维度

| 字段名 | 说明 |
|--------|------|
| **Year** | 发表年份 |
| **Documents** | 该年度的文献数量 |
| **Citations** | 该年度文献的总引用次数 |

### 数据来源

- **年份字段 (PY)**: Publication Year，从WOS或Scopus数据中提取
- **引文字段 (TC)**: Times Cited，取WOS和Scopus中的最大值

---

## 使用方法

### 方法1：使用完整工作流（推荐）

```bash
python3 run_complete_workflow.py --data-dir "/path/to/data"
```

年度统计会自动包含在 `workflow_complete_report.txt` 中。

### 方法2：单独运行合并去重

```bash
python3 merge_deduplicate.py wos.txt scopus_converted_to_wos.txt merged_deduplicated.txt
```

年度统计会包含在：
- 控制台输出（实时显示）
- `merged_deduplicated_report.txt`（保存到文件）

---

## 输出示例

### 控制台输出

```
============================================================
年度发文量及引文量统计
============================================================
Year       Documents       Citations
------------------------------------------------------------
2015       45              892
2016       67              1234
2017       89              1567
2018       102             1890
2019       118             2145
2020       134             2456
2021       145             2678
2022       156             2901
2023       167             3124
2024       89              1456
------------------------------------------------------------
Total      1112            20343
```

### 报告文件格式

在 `merged_deduplicated_report.txt` 中，年度统计表格会出现在：
- WOS-Scopus重复记录详情之后
- 合并策略说明之前

---

## 实际应用场景

### 1. 论文写作

在论文的 Methods 或 Results 部分引用：

> "We identified 1,112 publications from 2015 to 2024, with a total of 20,343 citations. The annual publication count increased from 45 in 2015 to 167 in 2023, demonstrating growing research interest in this field."

### 2. 趋势分析

通过年度数据可以：
- 识别研究领域的增长/衰退趋势
- 分析引用模式随时间的变化
- 确定研究热点的时间段

### 3. 数据质量检查

快速发现异常：
- 年份缺失（显示为 "Unknown"）
- 引用数异常低（可能数据不完整）
- 年份跨度是否符合预期

---

## 技术细节

### 统计逻辑

1. **年份提取**: 从每条记录的 `PY` 字段提取
2. **文档计数**: 按年份分组统计记录数
3. **引文累加**: 将同年份所有文献的 `TC` 值求和
4. **排序**: 按年份升序排列
5. **总计**: 计算所有年份的文档数和引文数总和

### 处理特殊情况

- **缺失年份**: 标记为 "Unknown"
- **缺失引文数**: 视为 0
- **无效引文数**: 转换失败时视为 0

### 代码位置

- **统计方法**: `merge_deduplicate.py:443-475` (`calculate_yearly_stats()`)
- **控制台输出**: `merge_deduplicate.py:502-524` (`print_report()` 中)
- **文件保存**: `merge_deduplicate.py:558-579` (报告文件生成中)

---

## 与其他工具的集成

### VOSviewer

生成的 `merged_deduplicated.txt` 可直接导入VOSviewer进行时间线分析。

### CiteSpace

在CiteSpace中可以进一步进行：
- Time Slicing Analysis（时间切片分析）
- Citation Burst Detection（引用突增检测）

### Bibliometrix (R)

可导出年度统计数据到CSV，与Bibliometrix结合：

```r
library(bibliometrix)
M <- convert2df("merged_deduplicated.txt", dbsource = "wos", format = "plaintext")
results <- biblioAnalysis(M)
plot(results, k = 10, pause = FALSE)
```

---

## 更新日志

**版本**: v2.1.1
**日期**: 2025-11-05
**作者**: Meng Linghan
**开发工具**: Claude Code

### 新增内容

- ✅ 年度发文量统计
- ✅ 年度引文量统计
- ✅ 格式化表格输出（控制台 + 文件）
- ✅ 总计行（汇总所有年份）

---

## 常见问题

### Q1: 年度统计中的引文数是什么时间点的？

A: 引文数 (TC) 是数据导出时的统计值。由于引用会随时间增长，不同时间导出的数据会有差异。

### Q2: "Unknown" 年份是什么？

A: 部分文献记录可能缺失年份字段 (PY)，这些记录会归入 "Unknown" 类别。

### Q3: 为什么我的引文数看起来偏低？

A: 可能的原因：
- 数据导出时间较早
- 某些数据库未包含引文信息
- 新发表文献尚未累积引用

### Q4: 可以导出为Excel或CSV吗？

A: 当前版本输出为文本格式。如需CSV，可以手动从报告文件中提取表格数据，或联系作者添加此功能。

---

## 反馈与建议

如有问题或建议，欢迎提交Issue：
- GitHub: [scopus-wos-tools](https://github.com/LeoMengTCM/scopus-wos-tools)

---

**Enjoy your bibliometric analysis! 📊✨**
