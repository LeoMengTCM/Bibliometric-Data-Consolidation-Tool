# 快速开始 - MultiDatabase 文献计量工具

**⏱ 5分钟上手 | 一键处理 | 专业输出**

---

## 📦 项目位置

```
/Users/menglinghan/Desktop/MultiDatabase
```

---

## 🚀 三步完成数据处理

### 第1步：准备数据文件

在你的数据目录中放入两个文件：
- `wos.txt` - WOS数据库下载的纯文本文件
- `scopus.csv` - Scopus数据库导出的CSV文件（需勾选所有字段）

**示例目录**：
```
/Users/menglinghan/Library/CloudStorage/.../2015-2024 NANO NSCLC IMMUNE/
├── wos.txt
└── scopus.csv
```

### 第2步：运行一键命令

```bash
cd /Users/menglinghan/Desktop/MultiDatabase

python3 run_ai_workflow.py \
  --data-dir "/path/to/your/data" \
  --year-range 2015-2024
```

**实际示例**：
```bash
python3 run_ai_workflow.py \
  --data-dir "/Users/menglinghan/Library/CloudStorage/OneDrive-共享的库-Onedrive/文献计量学/2015-2024 NANO NSCLC IMMUNE" \
  --year-range 2015-2024
```

### 第3步：获取结果

**推荐用于分析的文件**：
```
Final_Version_Year_Filtered.txt  ⭐ 导入VOSviewer/CiteSpace
Final_Version_Year_Filtered_analysis_report.txt  ⭐ 统计报告
```

---

## 📊 工作流自动完成的任务

1. ✅ **格式转换** - Scopus CSV → WOS纯文本
2. ✅ **WOS标准化** - 国家名、期刊名标准化
3. ✅ **AI智能补全** - 自动补全州代码、邮编、部门
4. ✅ **合并去重** - WOS+Scopus智能合并
5. ✅ **语言筛选** - 默认只保留英文文献
6. ✅ **机构清洗** - 合并重复机构、统一命名
7. ✅ **年份过滤** - 移除异常年份（Early Access等）
8. ✅ **统计分析** - 生成国家、机构、作者分布报告

**耗时**: 约5-10分钟（取决于数据量）

---

## 💡 常用参数

### 基本用法

```bash
python3 run_ai_workflow.py --data-dir "/path/to/data"
```

### 指定年份范围

```bash
python3 run_ai_workflow.py \
  --data-dir "/path/to/data" \
  --year-range 2015-2024
```

### 处理中文文献

```bash
python3 run_ai_workflow.py \
  --data-dir "/path/to/data" \
  --language Chinese
```

### 不使用AI补全（更快但质量较低）

```bash
python3 run_ai_workflow.py \
  --data-dir "/path/to/data" \
  --no-ai
```

---

## 📁 输出文件说明

工作流完成后，会在数据目录生成以下文件：

| 文件名 | 用途 | 推荐度 |
|--------|------|--------|
| `Final_Version_Year_Filtered.txt` | 最终数据文件（已过滤年份） | ⭐⭐⭐⭐⭐ |
| `Final_Version_Year_Filtered_analysis_report.txt` | 详细统计报告 | ⭐⭐⭐⭐⭐ |
| `Final_Version.txt` | 未过滤年份的数据 | ⭐⭐⭐ |
| `english_only.txt` | 语言筛选后的数据 | ⭐⭐⭐ |
| `merged_deduplicated.txt` | 合并去重后的数据 | ⭐⭐ |
| `scopus_enriched.txt` | AI补全后的Scopus数据 | ⭐⭐ |

**推荐用于VOSviewer/CiteSpace分析**: `Final_Version_Year_Filtered.txt`

---

## 🎯 典型使用场景

### 场景1：处理2015-2024年的纳米免疫治疗数据

```bash
python3 run_ai_workflow.py \
  --data-dir "/Users/menglinghan/Library/.../2015-2024 NANO NSCLC IMMUNE" \
  --year-range 2015-2024
```

**结果**：
- 原始660条 → 过滤后343条（移除Early Access和历史文献）
- 国家分布：Peoples R China (46.3%), United States (15.5%)
- 完整的机构、作者统计

### 场景2：只转换格式，不做其他处理

```bash
python3 scopus_to_wos_converter.py scopus.csv output.txt
```

### 场景3：单独进行年份过滤

```bash
python3 filter_by_year.py input.txt output.txt --year-range 2015-2024
```

---

## ⚙️ 系统要求

- **Python**: 3.6+
- **依赖**: `requests` (用于AI功能)
  ```bash
  pip3 install requests
  ```
- **操作系统**: macOS / Linux / Windows

---

## 📖 详细文档

- 📘 **完整使用指南**: `docs/快速使用指南.md`
- 📗 **AI补全详解**: `docs/AI补全系统完整总结.md`
- 📕 **年份过滤指南**: `docs/年份过滤使用指南.md`
- 📙 **机构清洗指南**: `docs/机构清洗使用指南.md`
- 📔 **项目结构**: `项目结构说明.md`

---

## 🆘 遇到问题？

### 问题1：找不到文件

**错误**: `FileNotFoundError: wos.txt`

**解决**: 确保数据目录中有 `wos.txt` 和 `scopus.csv` 文件

### 问题2：年份范围格式错误

**错误**: `ValueError: Invalid year range format`

**解决**: 使用正确格式 `--year-range 2015-2024` （连字符连接，无空格）

### 问题3：编码错误

**错误**: `UnicodeDecodeError`

**解决**: 确保Scopus CSV文件使用UTF-8编码导出

---

## ✨ 核心优势

1. **一键处理** - 单个命令完成所有步骤
2. **数据质量高** - AI智能补全+WOS标准化
3. **处理速度快** - 批量并发处理（20线程）
4. **成本极低** - AI缓存机制，二次运行零成本
5. **专业输出** - 直接用于VOSviewer/CiteSpace分析

---

**现在就开始处理你的数据吧！** 🎉

```bash
cd /Users/menglinghan/Desktop/MultiDatabase
python3 run_ai_workflow.py --data-dir "/your/data/path" --year-range 2015-2024
```
